import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.TopPage';

export default defineMessages({
  createdBy: {
    id: `${scope}.createdBy`,
    defaultMessage: 'Created by',
  },
});
